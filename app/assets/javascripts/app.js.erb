Hyrax.workEditor = function () {
    var element = $("[data-behavior='work-form']")
    if (element.length > 0) {
        var Editor = require('gpp/editor');
        new Editor(element).init();
    }
}

// Override sortAndPerPage to not auto reload on change
Hyrax.sortAndPerPage = function () {}